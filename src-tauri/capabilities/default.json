{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the KNULL wrapper.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}

